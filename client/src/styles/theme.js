import createTheme from "@material-ui/core/styles/createTheme";

export const theme = createTheme({
    palette: {
        type: 'dark',
        primary: {
            main: '#fff'
        }
    },
});
